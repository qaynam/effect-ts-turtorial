# このファイルを .env にコピーして、自分のデプロイ先 URL を書く(.env は git 管理外)。
#
# OG 画像の og:image / og:url は絶対 URL でないとクローラが解決できないため、
# ビルド時にこの値を HTML に焼き込む。未設定でもビルドは通るが、
# OG はリンク先で表示されない(警告が出る)。
#
# 末尾のスラッシュは不要。
SITE_URL=https://example.com
